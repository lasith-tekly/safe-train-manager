# Copy to .env.local for local development
# In production with nginx proxy, VITE_API_URL is not needed
# (nginx routes /api/ directly to backend)

# For direct API access without nginx:
# VITE_API_URL=https://your-amadeus-domain.com
VITE_API_URL=
